{
  "name": "sarma",
  "version": "0.0.3-alpha",
  "description": "A lightweight parser to parse chat message tokens written in pure Typescript",
  "repository": {
    "type": "git",
    "url": "https://github.com/bos-isler/sarma.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch:example": "cd example && yarn dev",
    "build:example": "cd example && yarn build",
    "watch:dev": "tsc --watch",
    "build": "tsc",
    "prepublish": "yarn build",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  }
}
