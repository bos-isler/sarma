{
  "name": "sarma",
  "version": "0.0.1-alpha",
  "description": "A lightweight parser to parse chat message tokens",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch:example": "cd example && yarn dev",
    "watch:dev": "tsc --watch",
    "build": "tsc",
    "prepublish": "yarn build",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  }
}
